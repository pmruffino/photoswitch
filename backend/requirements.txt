fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
redis>=5.2
pydantic>=2.0
argon2-cffi>=23.1
cryptography>=42.0
python-multipart>=0.0.9
httpx>=0.27
# iCloud connect / 2FA auth (icloud_router). Mainline pyicloud (modern 2FA +
# cookie-dir session API). Native deps resolve to glibc manylinux wheels in the
# Chainguard python -dev build stage.
pyicloud>=2.6
